<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Empty: the launcher icon is fully rendered in the background layer
         (@drawable/ic_launcher_background), so this layer stays transparent
         rather than adding a second element bound to the smaller safe zone. -->
</vector>
